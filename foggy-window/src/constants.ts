export const DEFAULTS = {
  condensation: 0.35,
  rainIntensity: 0.6,
  cityLightBreath: 0.50,
};

export const BG_TEXTURE_PATH = "/background.jpg";

// Parallax shift in UV units when mouse is at the corner (0 or 1).
// 0.025 = up to 2.5% of the image shifts in either direction.
export const PARALLAX_STRENGTH = 0.025;

